import { LoadingStyle, MainContainer } from '../styles/loadingStyles';

export default function Loading() {
  return (
    <MainContainer>
      <LoadingStyle></LoadingStyle>
    </MainContainer>
  );
}
